name: netcup
runtime: yaml

resources:
  myRandomResource:
    type: netcup:Random
    properties:
      length: 24
  myRandomComponent:
    type: netcup:RandomComponent
    properties:
      length: 24

outputs:
  output:
    value: ${myRandomResource.result}
